name: Test_Connection_on_Runner

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  Deploy:
    runs-on: [TestRP]
    
    steps:
      - name: Test Connect
        run: lscpu
              
